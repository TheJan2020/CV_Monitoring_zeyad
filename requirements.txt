# Config (.env)
python-dotenv>=1.0.0

# Step 1 — object detection (YOLO via Ultralytics)
ultralytics>=8.3.0
opencv-python>=4.8.0

# Step 2 — pose detection (MediaPipe legacy solutions API)
# 0.10.31+ removed mp.solutions on Windows; 0.10.14 is the last reliable build here.
mediapipe==0.10.14

# Step 3 — live web dashboard (workbench_activity.py --web)
flask>=3.0.0

# Step 4 — audio capture from RTSP (baby-monitor mode)
# Bundles a ready-to-use ffmpeg binary so we don't need a separate system install.
imageio-ffmpeg>=0.5.1
